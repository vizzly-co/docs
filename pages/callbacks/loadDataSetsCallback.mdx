import { Tab, Tabs } from "nextra-theme-docs";

# Load data sets

A function that returns a list of data sets that describe the available tables and fields available to the user.

### Return value

<Tabs items={["Type definition", "Example"]}>
<Tab>

```typescript
{
  // ID of the data set.
  id: string;

  // Public name of the data set, displayed to your users.
  name: string;

  // Fields that make up the data set.
  fields: {
    // Unique constant that identifies this field.
    id: string;

    // Public name of the field, displayed to your users.
    publicName: string;

    // Data type of the field
    dataType: "number" | "boolean" | "string" | "date_time";
  }
  [];
}
[];
```

</Tab>

<Tab>

```json
[
  {
    "id": "das_69948d88edaf4e55a864a0f25357139f",
    "name": "Orders",
    "fields": [
      {
        "dataType": "date_time",
        "publicName": "Order Date",
        "id": "fie_1"
      },
      {
        "dataType": "string",
        "publicName": "Status",
        "id": "fie_2"
      },
      {
        "dataType": "string",
        "publicName": "SKU",
        "id": "fie_3"
      },
      {
        "dataType": "number",
        "publicName": "Price",
        "id": "fie_4"
      }
    ]
  }
  // ...
]
```

</Tab>
</Tabs>
